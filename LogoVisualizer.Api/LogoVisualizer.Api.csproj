<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Used by dotnet user-secrets for local dev secrets -->
    <UserSecretsId>logovisualizer-api-secrets</UserSecretsId>
  </PropertyGroup>

  <ItemGroup>
    <!-- JWT authentication — tokens issued by the Master application -->
    <PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" Version="10.0.0" />

    <!-- OpenAPI / Swagger docs (auto-generated from controllers) -->
    <PackageReference Include="Swashbuckle.AspNetCore" Version="6.5.0" />

    <!-- Server-side image compositing for PNG export -->
    <PackageReference Include="SixLabors.ImageSharp" Version="3.1.5" />

    <!-- IP rate limiting for public viewer/upload endpoints -->
    <PackageReference Include="AspNetCoreRateLimit" Version="5.0.0" />

    <!-- Required by dotnet-ef CLI when this is the startup project -->
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="10.0.0">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\LogoVisualizer.Data\LogoVisualizer.Data.csproj" />
  </ItemGroup>

</Project>
